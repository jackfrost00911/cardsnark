---
layout: default
title: Credit Card Rewards Calculator - The Math Doesn't Lie
description: Stop guessing. Enter your monthly spending and see exactly which credit card earns you the most money in 2025.
---

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

<style>
    /* Calculator Specific Styles */
    .calc-wrapper { font-family: 'Inter', sans-serif; color: #0f172a; }
    
    /* Input Styling */
    .spend-input-group {
        position: relative;
        transition: all 0.2s;
    }
    .spend-input {
        width: 100%;
        padding: 1rem 1rem 1rem 3rem; /* Space for icon */
        border: 1px solid #e2e8f0;
        border-radius: 12px;
        font-size: 1.1rem;
        font-weight: 600;
        color: #1e293b;
        transition: all 0.2s;
        background: #f8fafc;
    }
    .spend-input:focus {
        background: white;
        border-color: #4f46e5;
        box-shadow: 0 0 0 4px #e0e7ff;
        outline: none;
    }
    .input-icon {
        position: absolute;
        left: 1.2rem;
        top: 50%;
        transform: translateY(-50%);
        color: #94a3b8;
        font-size: 1.1rem;
        transition: color 0.2s;
    }
    .spend-input:focus + .input-icon { color: #4f46e5; }

    /* Card Result Styling */
    .card-row {
        display: grid;
        grid-template-columns: 60px 1fr auto;
        gap: 1rem;
        padding: 1.5rem;
        border-bottom: 1px solid #f1f5f9;
        align-items: center;
        transition: background 0.2s;
    }
    .card-row:last-child { border-bottom: none; }
    
    .winner-card {
        background: #f0fdf4; /* Light Green */
        border: 2px solid #16a34a;
        border-radius: 16px;
        margin-bottom: 2rem;
        position: relative;
        box-shadow: 0 10px 30px -5px rgba(22, 163, 74, 0.2);
    }
    .winner-badge {
        position: absolute;
        top: -12px;
        left: 50%;
        transform: translateX(-50%);
        background: #16a34a;
        color: white;
        font-size: 0.75rem;
        font-weight: 800;
        text-transform: uppercase;
        padding: 4px 12px;
        border-radius: 20px;
        letter-spacing: 0.05em;
    }

    /* Animations */
    .slide-up { animation: slideUp 0.4s ease-out forwards; }
    @keyframes slideUp { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
</style>

<div class="calc-wrapper bg-white min-h-screen pb-20">

    <div class="bg-slate-900 text-white pt-20 pb-24 px-4 text-center relative overflow-hidden">
        <div class="absolute top-0 left-0 w-full h-full opacity-10 bg-[url('https://www.transparenttextures.com/patterns/cubes.png')]"></div>
        <div class="max-w-3xl mx-auto relative z-10">
            <span class="inline-block py-1 px-3 rounded-full bg-indigo-500/20 border border-indigo-500/50 text-indigo-200 text-xs font-bold uppercase tracking-wider mb-6">Real-Time Calculator</span>
            <h1 class="text-4xl md:text-6xl font-black mb-6 tracking-tight">The Math Doesn't Lie.</h1>
            <p class="text-lg text-slate-300 leading-relaxed max-w-2xl mx-auto">
                Enter your monthly spending below. We'll run the numbers against the best cards in the market instantly. No "submit" button required.
            </p>
        </div>
    </div>

    <div class="max-w-6xl mx-auto px-4 -mt-16 relative z-20">
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-8">
            
            <div class="lg:col-span-5 space-y-6">
                <div class="bg-white rounded-2xl shadow-xl border border-gray-100 p-6 md:p-8">
                    <h3 class="text-lg font-bold text-slate-900 mb-6 flex items-center">
                        <i class="fa-solid fa-sliders text-indigo-600 mr-2"></i> Monthly Spend
                    </h3>
                    
                    <div class="space-y-5">
                        <div class="spend-input-group">
                            <label class="block text-xs font-bold text-slate-500 uppercase mb-1 ml-1">Dining & Delivery</label>
                            <input type="number" id="dining" class="spend-input" placeholder="0" oninput="calculate()" value="500">
                            <i class="fa-solid fa-utensils input-icon"></i>
                        </div>

                        <div class="spend-input-group">
                            <label class="block text-xs font-bold text-slate-500 uppercase mb-1 ml-1">Groceries</label>
                            <input type="number" id="groceries" class="spend-input" placeholder="0" oninput="calculate()" value="400">
                            <i class="fa-solid fa-cart-shopping input-icon"></i>
                        </div>

                        <div class="spend-input-group">
                            <label class="block text-xs font-bold text-slate-500 uppercase mb-1 ml-1">Travel (Flights/Hotels)</label>
                            <input type="number" id="travel" class="spend-input" placeholder="0" oninput="calculate()" value="200">
                            <i class="fa-solid fa-plane input-icon"></i>
                        </div>

                        <div class="spend-input-group">
                            <label class="block text-xs font-bold text-slate-500 uppercase mb-1 ml-1">Gas & Transit</label>
                            <input type="number" id="gas" class="spend-input" placeholder="0" oninput="calculate()" value="150">
                            <i class="fa-solid fa-gas-pump input-icon"></i>
                        </div>

                        <div class="spend-input-group">
                            <label class="block text-xs font-bold text-slate-500 uppercase mb-1 ml-1">Everything Else</label>
                            <input type="number" id="other" class="spend-input" placeholder="0" oninput="calculate()" value="1000">
                            <i class="fa-solid fa-receipt input-icon"></i>
                        </div>
                    </div>
                    
                    <div class="mt-6 p-4 bg-slate-50 rounded-xl text-xs text-slate-500 leading-relaxed border border-slate-100">
                        <i class="fa-solid fa-circle-info text-indigo-500 mr-1"></i>
                        <strong>Note:</strong> We value points (Amex/Chase) at 1 cent each for a fair comparison against cash back. In reality, they can be worth much more if used for travel.
                    </div>
                </div>
            </div>

            <div class="lg:col-span-7">
                <div class="bg-white rounded-2xl shadow-xl border border-gray-100 overflow-hidden">
                    <div class="bg-slate-50 px-6 py-4 border-b border-gray-200 flex justify-between items-center">
                        <h3 class="font-bold text-slate-900">Annual Rewards Value</h3>
                        <span class="text-xs font-bold text-slate-400 uppercase tracking-wider">Net Value (Post-Fee)</span>
                    </div>
                    
                    <div id="results-list" class="p-6">
                        </div>
                </div>
            </div>

        </div>
    </div>

</div>

<script>
    // Card Database
    const cards = [
        {
            name: "Amex Gold Card",
            fee: 325,
            img: "bg-yellow-500", 
            calc: (d, g, t, gas, o) => {
                // 4x Dining/Grocery (capped at 25k/yr for grocery but simplistic here), 3x Flights
                const rewards = ((d * 4) + (g * 4) + (t * 3) + (gas * 1) + (o * 1)) * 12 * 0.01;
                // Add approx $240 in dining credits (Uber/Grubhub) to be fair to fee
                return rewards + 240; 
            },
            note: "4x on Food is unbeatable."
        },
        {
            name: "Chase Sapphire Preferred",
            fee: 95,
            img: "bg-blue-700",
            calc: (d, g, t, gas, o) => {
                // 3x Dining, 3x Online Grocery (Assume 50% of G is online?), 2x Travel
                // Simplified: 3x Dining, 2x Travel, 1x else. + $50 hotel credit
                const rewards = ((d * 3) + (t * 2) + (g * 1) + (gas * 1) + (o * 1)) * 12 * 0.01;
                return rewards + 50; 
            },
            note: "Great starter travel card."
        },
        {
            name: "Capital One Savor",
            fee: 0,
            img: "bg-orange-600",
            calc: (d, g, t, gas, o) => ((d * 0.03) + (g * 0.03) + (t * 0.01) + (gas * 0.01) + (o * 0.01)) * 12,
            note: "Best free card for foodies."
        },
        {
            name: "Citi Double Cash",
            fee: 0,
            img: "bg-green-600",
            calc: (d, g, t, gas, o) => ((d + g + t + gas + o) * 0.02) * 12,
            note: "Simple 2% on everything."
        },
        {
            name: "Amex Blue Cash Preferred",
            fee: 95,
            img: "bg-blue-500",
            calc: (d, g, t, gas, o) => {
                // 6% grocery capped at $6k/yr ($500/mo)
                const groceryEarnings = (Math.min(g, 500) * 0.06) + (Math.max(0, g - 500) * 0.01);
                // 3% gas, 1% else
                return ((d * 0.01) + groceryEarnings + (t * 0.01) + (gas * 0.03) + (o * 0.01)) * 12;
            },
            note: "6% on groceries (capped)."
        },
        {
            name: "Capital One Venture X",
            fee: 395,
            img: "bg-gray-800",
            calc: (d, g, t, gas, o) => {
                // 2x Flat on everything
                const rewards = ((d + g + t + gas + o) * 2) * 12 * 0.01;
                // Add 10k anniversary bonus ($100) + $300 travel credit (only if they spend enough on travel)
                const travelCredit = Math.min(300, t * 12);
                return rewards + 100 + travelCredit;
            },
            note: "Includes $400 in annual credits."
        },
        {
            name: "Chase Freedom Unlimited",
            fee: 0,
            img: "bg-indigo-500",
            calc: (d, g, t, gas, o) => ((d * 0.03) + (g * 0.015) + (t * 0.015) + (gas * 0.015) + (o * 0.015)) * 12, 
            note: "3% Dining + 1.5% Base."
        }
    ];

    function calculate() {
        const d = parseFloat(document.getElementById('dining').value) || 0;
        const g = parseFloat(document.getElementById('groceries').value) || 0;
        const t = parseFloat(document.getElementById('travel').value) || 0;
        const gas = parseFloat(document.getElementById('gas').value) || 0;
        const o = parseFloat(document.getElementById('other').value) || 0;

        // Calculate Net Value (Rewards - Fee)
        const results = cards.map(card => {
            const grossRewards = card.calc(d, g, t, gas, o);
            const netValue = grossRewards - card.fee;
            return { ...card, netValue, grossRewards };
        });

        // Sort by Net Value
        results.sort((a, b) => b.netValue - a.netValue);

        // Render
        const list = document.getElementById('results-list');
        list.innerHTML = '';

        results.forEach((card, index) => {
            const isWinner = index === 0;
            let html = '';
            
            if (isWinner) {
                html = `
                <div class="winner-card p-6 flex flex-col md:flex-row items-center gap-6 slide-up">
                    <div class="winner-badge">#1 Choice</div>
                    <div class="w-16 h-10 rounded bg-gradient-to-r from-green-500 to-emerald-700 shadow-md flex-shrink-0"></div>
                    <div class="flex-grow text-center md:text-left">
                        <h4 class="text-xl font-black text-slate-900">${card.name}</h4>
                        <p class="text-sm text-green-700 font-medium"><i class="fa-solid fa-check mr-1"></i> ${card.note}</p>
                    </div>
                    <div class="text-right">
                        <div class="text-3xl font-black text-green-600">$${card.netValue.toFixed(0)}</div>
                        <div class="text-xs text-slate-500 font-bold uppercase">Net Annual Value</div>
                    </div>
                </div>
                `;
            } else {
                html = `
                <div class="card-row slide-up" style="animation-delay: ${index * 50}ms">
                    <div class="w-12 h-8 rounded ${card.img} opacity-80"></div>
                    <div>
                        <h4 class="font-bold text-slate-700">${card.name}</h4>
                        <p class="text-xs text-slate-400">Fee: $${card.fee}/yr</p>
                    </div>
                    <div class="text-right">
                        <div class="font-bold text-slate-900 text-lg">$${card.netValue.toFixed(0)}</div>
                    </div>
                </div>
                `;
            }
            list.innerHTML += html;
        });
    }

    // Run on load
    window.addEventListener('DOMContentLoaded', calculate);
</script>
